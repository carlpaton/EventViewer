namespace Common
{
    public class CategoryModel
    {
        //This should actually be part of the 'Business Domain' but its fine here for now
        public enum Categorys
        {
            UserLogin = 1,
            DebtorsCapture,
            CreditorsCapture
        }
    }
}
